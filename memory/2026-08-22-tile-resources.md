# 首批瓦片资源及导入规范

- 修改内容：依据《争源_瓦片资源统一生成规范_v1.1.md》生成草地和 3×3 基地资源，尺寸分别为 32×32 和 96×96。
- 影响范围：草地为无缝不透明 Ground Tile；基地为透明 PNG。两者使用统一像素密度、正交俯视像素风和硬像素边缘，并采用 Nearest 导入；Ground Tile 可按需开启 Repeat，基地保持关闭。
- 资源分类：草地位于 `art/ground/`，基地位于 `art/structures/`。
- 验证方式：已检查 PNG 尺寸、RGBA 模式和透明度，并同步更新 Godot 场景与脚本引用。
