# 鼠标滚轮视角缩放

- 修改内容：`EdgeScrollCamera` 支持鼠标滚轮缩放；向上滚动放大，向下滚动缩小，并以鼠标指向的世界位置为缩放中心。
- 使用约定：默认缩放范围为 `0.35`～`1.5`，每档倍率为 `1.1`；缩放后继续按视口、缩放值与地图尺寸限制相机边界。
- 交互约定：鼠标位于阻挡交互的 UI 上或相机处理被建造预览临时停用时，不响应滚轮缩放；允许边缘滚屏的 UI 区域也允许缩放。
- 验证方式：通过 Godot MCP 使用 Godot 4.7.2、Forward Mobile 启动主项目，调试输出无脚本解析错误和资源加载错误。
